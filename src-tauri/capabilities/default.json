{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions granted to every Prayer Bar window (bar, panel, settings). This app has no remote/untrusted content, so one shared capability across all windows is appropriate.",
  "windows": ["bar", "panel", "settings"],
  "permissions": [
    "core:default",
    "core:window:allow-set-always-on-top",
    "core:window:allow-set-position",
    "core:window:allow-start-dragging",
    "store:default",
    "notification:default",
    "autostart:default",
    "global-shortcut:default",
    "dialog:default"
  ]
}
